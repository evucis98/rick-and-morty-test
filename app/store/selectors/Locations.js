export const selectLocations = ({ locations: { location } }) => location;
export const selectInfo = ({ locations: { info } }) => info;
export const selectType = ({ locations: { type } }) => type;
export const selectDimension = ({ locations: { dimension } }) => dimension;
export const selectPage = ({ locations: { page } }) => page;
